---
origin: authored
authored_by: user + claude (R2 의도 복원 — D-014)
status: draft (human 게이트 미판정)
source_commit: 370caeaf1141
---

# code-tutor-ai 유스케이스 (복원)

> AC- 항목은 대응 엔드포인트(EP-, 역추출 api-spec.md 실존 id)를 인용한다.
> UI 전용 AC 는 EP-N/A(사유) 명시. P1 = 핵심 축(가입→문제→제출→채점), P2 = 유예.

## 페르소나

- persona **학습자 (지민)**: 알고리즘을 독학하는 취준생 — 문제를 풀고 즉시 채점 피드백을 원한다.
- persona **운영자 (셀프호스터)**: 데모를 자기 서버에 docker-compose 로 띄우는 개발자 — 기동·상태 확인이 한 번에 되기를 원한다.

## P1 — 핵심 축 (REQ-010 · REQ-011 · REQ-012)

### UC-01 계정 생성과 로그인 (REQ-010)
- Given 미가입 이메일 / When 가입 양식 제출 / Then 계정 생성·201
  - AC-001 유효 입력으로 회원가입 성공 — EP-027
  - AC-002 중복 이메일 가입 시 409 거부 — EP-027
- Given 가입된 계정 / When 로그인 / Then 토큰 발급, 이후 요청 인증
  - AC-003 정상 로그인으로 토큰 수령 — EP-028
  - AC-004 로그인 사용자가 내 프로필 조회 — EP-031
  - AC-005 잘못된 자격증명 로그인 401 거부 (실패 GWT) — EP-028

### UC-02 문제 탐색과 조회 (REQ-011)
- Given 게시된 문제 존재 / When 목록·상세 조회 / Then 문제·예시 표시
  - AC-010 문제 목록 페이지네이션 조회 — EP-034
  - AC-011 문제 상세(설명·예시 테스트케이스) 조회 — EP-035
  - AC-012 존재하지 않는 문제 404 (실패 GWT) — EP-035

### UC-03 코드 제출과 채점 (REQ-011 · REQ-012)
- Given 로그인 상태·문제 상세 / When 코드 제출 / Then 격리 실행·채점 결과
  - AC-020 제출 생성 201, 제출 id 수령 — EP-042
  - AC-021 제출 상세에서 채점 결과(통과/실패 테스트케이스) 확인 — EP-044
  - AC-022 내 제출 이력 조회 — EP-046
  - AC-023 임의 코드의 단발 실행(제출 없이) — EP-014
  - AC-024 무한루프성 코드는 시간 제한으로 종료·실패 처리 (실패 GWT) — EP-014

### UC-04 운영자 기동·관측 (REQ-003 · REQ-033)
- Given docker-compose up / When 헬스 확인 / Then 서비스 정상 응답
  - AC-030 /api/health 200 — EP-063
  - AC-031 프론트 첫 페이지 렌더 (브라우저) — EP-N/A(프론트 라우팅 — e2e 로 검증)

## P2 — 유예 (온보딩 후 확장 — REQ 커버리지 등록)

다음 도메인은 코드 실물이 존재하나(각 REQ evidence 참조) 유스케이스 상세 복원을 P2 로 유예한다:
REQ-013 (AI 튜터 — 스텁 REQ-004 적용), REQ-014 (게이미피케이션), REQ-015 (로드맵),
REQ-016 (플레이그라운드), REQ-017 (실시간 협업), REQ-018 (디버거), REQ-019 (타이핑),
REQ-020 (시각화), REQ-021 (성능), REQ-022 (ML 스텁), REQ-023 (프론트 전체 페이지).
비기능 REQ-030 · REQ-031 · REQ-032 는 하네스 게이트가 직접 소유(유스케이스 아님).
목적 요구 REQ-001(하네스 검증)·REQ-002(프로덕션급 판정)는 특정 UC 가 아니라 전 UC 를
관통한다 — 추적은 하네스 원장(게이트 이벤트)과 비기능 게이트 전량 PASS 가 소유한다.
