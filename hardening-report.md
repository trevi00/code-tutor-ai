# 하드닝 라이트 패스 (REQ-003 셀프호스팅 데모 수위)

> 스코프: 공개 서비스 수위(WAF·멀티테넌시 등)는 Non-Goal(REQ-003). 데모 수위에서
> 허용/조치/사람 결정을 구분한다.

## 점검 결과 (코드 근거)

| 항목 | 현황 | 판정 |
|---|---|---|
| JWT 시크릿 | 기본값 `change-this-…` — 단, `config.py:80` 이 **prod 환경에서 기본값 거부 가드 보유** | 데모 허용 (가드 실존 확인) |
| DB/Grafana 자격 | `.env.example` 기본값(postgres/postgres, admin/admin) + prod 섹션 주석 안내 존재 | 데모 허용 — 외부 노출 시 교체 안내 문서화됨 |
| CORS | `settings.CORS_ORIGINS` 설정 기반(와일드카드 하드코드 아님) | 허용 |
| 포트 노출 | db 5433·redis 6380 이 호스트에 노출 | 허용 — 로컬 개발 편의 우선, 노출 유지 (사용자 확정 2026-08-14, 舊 OPEN-H1) |
| rate limiter | 로그인 5/min·burst 3 (인메모리·IP 키) | 허용 — 멀티 인스턴스 승격 시 재검토 각주 |

## 해소된 결정 (사용자 확정 2026-08-14)

- ~~OPEN-H1~~ → **포트 노출 유지** — 로컬 개발 편의 우선. 데모=내부망 전제이며,
  외부 공개 전환 시 compose 의 호스트 포트 바인딩 제거를 공개 체크리스트에 포함.
- ~~OPEN-H2~~ → **`.env` prod 섹션은 외부 공개 직전 적용** — 그 전까지 개발 기본값
  유지(prod 가드가 기본값 거부를 강제하므로 미적용 상태로 prod 기동 불가 — 안전).

## 외부 공개 전 체크리스트 (OPEN 해소가 남긴 실행 항목)

1. `.env` prod 섹션 적용 (JWT/DB/Grafana 자격 교체)
2. docker-compose 호스트 포트 바인딩(5433/6380) 제거 또는 내부망 한정
3. CORS_ORIGINS 를 공개 도메인으로 한정

## RESULT
데모 수위 하드닝 판정: 차단급 결함 0건 · 허용 5 · OPEN 0 (전 항목 해소 — 숨김 없음).
